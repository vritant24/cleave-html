"use strict";

const CONFIG_FILE   = "cleave-html-config.json";
const FILE_TYPE     = "utf-8";
const EOF           = "EOF";

module.exports = Object.freeze({
    CONFIG_FILE,
    FILE_TYPE,
    EOF,
});
